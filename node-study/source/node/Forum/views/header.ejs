<!--这个页面是共享的，所有的页面都include这个页面-->
<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">FORUM</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <%
                if(login){
                %>
                    <li <%if(active=="我的主页"){%> class="active" <%}%>><a href="/user/<%= username %>">我的主页</a></li>
                <% } %>
                <li <%if(active=="成员列表"){%> class="active" <%}%>><a href="/userlist">成员列表</a></li>
            </ul>
            <%
            if(login){
            %>
            <ul class="nav navbar-nav navbar-right">
                <li><a>欢迎您！<%= username %></a></li>
                <li><a href="/out">退出</a></li>
            </ul>
            <%
            }else{
            %>
            <ul class="nav navbar-nav navbar-right">
                <li <%if(active=="注册"){%> class="active" <%}%>><a href="/regist">注册</a></li>
                <li <%if(active=="登录"){%> class="active" <%}%>><a href="/login">登录</a></li>
            </ul>
            <%
            }
            %>
        </div>
        <!--/.navbar-collapse -->
    </div>
</nav>
